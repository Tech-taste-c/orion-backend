<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <style>
      body { font-family: Arial, sans-serif; text-align: center; }
      h1 { font-size: 32px; margin-top: 80px; }
      p  { font-size: 18px; }
    </style>
  </head>
  <body>
    <h1>{{certificateName}}</h1>
    <p>This is to certify that <strong>{{studentName}}</strong></p>
    <p>has successfully completed the course <strong>{{courseName}}</strong></p>
    <p>Date: {{todayDate}}</p>
    <p>Student ID: {{studentId}}</p>
  </body>
</html>